services:
  - type: web
    name: cdms-v2
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app --workers 2 --threads 4 --bind 0.0.0.0:$PORT
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: cdms-db
          property: connectionString
      - key: CORS_ORIGINS
        value: https://cdms.onrender.com

databases:
  - name: cdms-db
    databaseName: cdms
    user: cdms_user
    plan: free
